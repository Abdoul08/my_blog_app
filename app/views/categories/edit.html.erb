<h1>Page de modification des catégories</h1> 
<% if @categorie.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@categorie.errors.count, "error") %> prohibited this <%= @categorie.name %> from being saved:</h2>
    <ul>
    <% @categorie.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
 <%= form_with(model: @categorie, local: true) do |f| %>
   
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit  %>
  </div>
 <% end %>

 <%= link_to t('Return'),categories_path%>